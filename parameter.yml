semantic_model_binding:
    default:
        connection_id:
            DEV: ${{ DEV_WORKSPACE_ID }}
            TEST: ${{ TEST_WORKSPACE_ID }}
 
    models:
        - semantic_model_name: "test_2_semantic_model"
          connection_id:
              DEV: ${{ DEV_WORKSPACE_ID }}
              TEST: ${{ TEST_WORKSPACE_ID }}